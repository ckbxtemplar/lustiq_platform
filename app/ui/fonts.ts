import { Roboto, Barlow } from 'next/font/google';
export const roboto = Roboto({ weight: ['400', '700'], subsets: ['latin'] });
export const barlow = Barlow({  weight: ['400', '600', '700', '800'],  subsets: ['latin']});